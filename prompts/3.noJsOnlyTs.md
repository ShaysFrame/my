okay I'm not really fan of javascript that much, although under the hood I know everything is indeed javascript, but I prefer typescript I think. what do you think we should also probably have this as a rule.

---

Good call — TypeScript is the right default for a portfolio that's meant to impress. Let me check the current SKILL.md first. 

Searched for text `Component language|typescript|javascript|jsx` (`**/tools/portfolio-skill/SKILL.md`), 2 results

Read [](file:///Users/shay/Dev/my/tools/portfolio-skill/SKILL.md#80-80), lines 80 to 130

Two things to do: update the SKILL.md rule, and rename/convert `Greeting.jsx` → `Greeting.tsx` since that's the only JS file in your components. 

Read [](file:///Users/shay/Dev/my/pub-shay-blog-astro/src/components/Greeting.jsx)

It's using Preact. I'll convert it to TypeScript and update the SKILL.md rule simultaneously. 

Replacing 2 lines with 3 lines in [](file:///Users/shay/Dev/my/tools/portfolio-skill/SKILL.md)


```

```
Now convert the component and remove the old file: 

Created [](file:///Users/shay/Dev/my/pub-shay-blog-astro/src/components/Greeting.tsx)

Ran terminal command:  rm /Users/shay/Dev/my/pub-shay-blog-astro/src/components/Greeting.jsx && grep -r "Greeting.jsx\|Greeting" /Users/shay/Dev/my/pub-shay-blog-astro/src --include="*.astro" --include="*.ts" --include="*.tsx" -l

Searched for text `Greeting` (`**/pub-shay-blog-astro/src/pages/index.astro`), 4 results

The import uses no extension (`../components/Greeting`) so it resolves to the `.tsx` automatically — no changes needed there.

Let me quickly verify the tsconfig handles JSX correctly for Preact: 

Read [](file:///Users/shay/Dev/my/pub-shay-blog-astro/tsconfig.json)

Everything is already configured correctly — `jsx: react-jsx` + `jsxImportSource: preact` already in the tsconfig.

---

**Summary of what changed:**

- SKILL.md — updated Tech Stack to enforce TypeScript (`.ts`, `.tsx`) everywhere, never `.js`/`.jsx`
- src/components/Greeting.tsx — converted from `.jsx` to `.tsx` with a proper `GreetingProps` interface and typed state
- Greeting.jsx deleted; the existing import in index.astro needs no change since it imports without extension

Your tsconfig was already set up for this (`strict` mode + Preact JSX transform), so no config changes were needed.

Made changes.